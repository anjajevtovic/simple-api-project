This repository contains simple api project written in Python.

Connection to the GitHub is created by 'github search api', query is made to find all repositories
with minimum number of stars (the number is being passed as an argument) and specific languages 
(specified trough argument as well). All the responses are being printed out.

